export const metadata = {
  title: "Australian Standard Pallet Supplier Victoria | LMA Pallets",
  description:
    "LMA Pallets supplies Australian Standard pallets, second hand pallets, standard export pallets and custom pallets across Victoria with delivery to your door.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
